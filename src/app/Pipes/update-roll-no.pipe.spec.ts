import { UpdateRollNoPipe } from './update-roll-no.pipe';

describe('UpdateRollNoPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateRollNoPipe();
    expect(pipe).toBeTruthy();
  });
});
